# mkdocs build output
/site/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local credentials — never commit (kept outside the repo)
.env
secrets.env

.coverage
.coverage.*
htmlcov/

# Local symlinks to sibling repos (CI checks these out itself)
.sdk
.integrations

# Stale local build snapshot (CI builds and deploys site/ itself)
/publish/
